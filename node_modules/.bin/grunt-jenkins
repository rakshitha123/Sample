#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jenkins/bin/grunt-jenkins" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jenkins/bin/grunt-jenkins" "$@"
  ret=$?
fi
exit $ret
